"""Core domain logic — Segment, Pipeline, ICU parsing, TM, validators.

Cycle 0 ships an empty `core` package as a placeholder so the layout is
in place. Concrete modules (`segment.py`, `icu.py`, `pipeline.py`, plus
`adapters/`, `tm/`, `validators/` subpackages) land in cycle 1.
"""
